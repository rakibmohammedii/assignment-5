function getInputValueById(id){
    const inputValue = document.getElementById(id).value;
    return inputValue;
};

function getDivValueById(id){ 
    const divValue = document.getElementById(id).innerText;
    return divValue;
};